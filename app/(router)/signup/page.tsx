import SignupForm from "@/app/components/Forms/SignupForm";

const Signup = () => {
  return (
    <section className="flex justify-center items-center  min-h-screen bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-stone-800 via-zinc-900 to-black">
      <SignupForm />
    </section>
  );
};

export default Signup;
